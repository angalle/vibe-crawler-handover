# 📦 배포 내역 — 2026.04.10

> **브랜치**: `prod`  
> **루트 프로젝트 배포 커밋**: `3b99d2cf` (최신 주요 커밋 기준)
> **Backend 서브모듈 커밋 반영**: `a8e0f499` -> `2625f98e`
> **Frontend 서브모듈 커밋 반영**: `a095c2a1` -> `1b28b6b6`

---

## 변경 요약

### Backend
- 수동실행 버그 수정 및 상태 변이 코드/상태 변화 오류 전면 수정
- JOB에 `IDLE` 상태를 새로 추가하고 `Pending` 상태 진입 시 이중 실행 방지 처리 적용
- 프로젝트 생성/수정 API 배포
- 대시보드 통계/로딩 에러 수정 및 로컬 경로 수정
- 1차 API 전송 로직 추가 (카테고리 값이 미디어 값으로 전송되도록 시스템 연계 수정)
- 싱글턴 패턴 삭제 확정 및 트랜잭션 롤백 로직 추가
- DI(의존성 주입) 검증 및 상태 변경에 따른 유닛테스트 대거 보완

### Frontend
- 상태값 변경(IDLE 추가 등)에 따른 프론트엔드 UI 연동 적용
- 1차 API 전송 연계 시 카테고리값이 미디어값으로 전송되도록 전송 모델 수정
- 검색 시 키바인딩 오류 수정
- 카테고리 메뉴: 새 카테고리 추가 버튼을 스크롤 영역 밖으로 분리 (검색 시 클릭 위치 밀림 현상 방지)
- 인크로스 매체 선택: 이미 등록된 매체 선택 시 불필요한 모달 팝업 방지 (`isRegistered` 플래그 적용)

## 수정 파일

### Backend
*(약 220개의 파일이 변경/유지보수 되었습니다. 주요 도메인별 디렉토리 세부 내역은 다음과 같습니다.)*

- **`workspace/project/job/crawling/` (크롤링 세션 핵심 로직)**
  - `start_crawling_service.py` 외 다수: 상태값(IDLE 등 추가) 프로세스 및 이중 Pending(실행) 방지 로직 전면 적용
  - `post_crawling_api_service.py`, `data_upload_adapter.py` 등: 1차 수집 API 데이터 전송 규격 수정 (시스템상 카테고리값이 미디어값으로 연동/전송되도록 기능 패치)
  - `crawler_hotfix_service.py`, `docker_crawler_adapter.py`: 수동 실행 버그, 상태 변환 오류 수정 및 도커 로컬 경로 동기화
- **`workspace/project/` (프로젝트 관리 본체)**
  - `project_controller.py`, `create_project_service.py`, `update_project_service.py` 등: 프로젝트 생성/수정/조회 API 엔드포인트 수정 및 처리 로직 보완
- **`workflow/` (AI 코어 워크플로우 엔진)**
  - `execute_workflow_service.py`, `workflow_engine_service.py`: 워크플로우 실행 엔진 보완 
  - `adapter/output/nodes/` 전반 (`rag_facade_adapter`, `vlm_facade_adapter`, `loop_node_adapter` 등): AI 노드 컴포넌트 내 상태 오류 에러 일괄 수정
- **`workspace/project/job/ai_auto_fix/` 및 `scripts/` (자동화 보정)**
  - `analyze_failure_service.py`, `apply_modified_script_service.py`를 포함한 거의 전구역: AI 기반 스크립트 수정 및 작업내용 상태 변화 에러 처리 개선
- **`shared/database.py` 및 전역 설정**
  - DB 세션 관리 고도화: 트랜잭션 오염 방어용 최상위 `rollback()` 롤백 처리 로직 구성 완료
  - **레거시 제거**: 싱글턴 패턴 체계 100% 제거 후 의존성 주입(DI) 체계로 교체 연동 완료
- **`tests/` 및 `workspace/project/mdc_generator/` (테스트 보완)**
  - 상태 변화 및 DI(의존성 주입) 격리 검증을 완전 보장하기 위한 15개 이상의 유닛 테스트 대거 보완 및 통과 완료

### Frontend
- `frontend/src/components/CategorySelect.vue` 등 카테고리 및 매체 선택 관련 컴포넌트 — 추가 버튼 분리 및 모달 플래그 적용
- 상태값 관련 스토어 및 유틸 로직 — IDLE 및 Pending UI 상태처리 적용

## DB 스키마 (변경 시에만)
- 해당사항 없음

## 운영 이슈 (발생 시에만)
- 해당사항 없음
